# Reflection: 2026-04-14 16:07:20 UTC

- Session Key: agent:dc-channel--1476866394556465252:discord:channel:1490027059165008162
- Session ID: 9c0a6f1e-6c5a-41a2-a64a-e5344c9269bb
- Command: reset
- Error Signatures: (none)
## Context (session background)
- memory-lancedb-pro B-2（全域 Retrieval 鄰居擴展）討論：維護者確認 B-2 方向後，發現 5 個實作缺口 Q5-Q9，張貼至 Issue #538 等回覆
- PR #500 合併進度追蹤（rwmjhb 等 maintainer 操作）
- PR #523 neighbor timestamp bug 討論
- PR #503/#523/#529 三個 PR 進度管理
- Issue #538 Q5-Q9：MMR 消滅 neighbors（🔴）、vectorOnlyRetrieval 無 BM25（🔴）、neighbor scope boundary（🟡）、Phase 4 依賴被跳過（🟡）、BM25 neighbor lookup input scope（🟡）

## Decisions (durable)
- `docs/AI_WORKFLOW_MANUAL.md` 是 AI 工作流程的唯一 canonical 檔案，多個舊版指南（WORKFLOW_GUIDE.md、JAMES_WORKFLOW_HANDBOOK.md、AI_WORKFLOW_MANUAL.md 等）已整合廢棄，未來新增規則統一寫入此檔
- Sub-agent 完成後 main session 必須完整驗證：git show HEAD --stat + 抽查關鍵檔案 + 測試通過
- 維護者回覆分析時，先查 memory/ 中現有設計文件再做分析（R11）
- 重要外部回覆立即結構化，區分「已確認」vs「待確認」（R14）
- 「架構決定」和「實作方向」未確認前，不先寫程式碼（R15）

## User model deltas (about the human)
- 繁體中文回覆，溫暖但有重點
- 要求明確、不重複：同一主題不要每次討論串產生不同檔案，要整合進 canonical 檔案
- 希望每次做完事情都有記錄和檢討，並同步蒸餾成可重複使用的規則
- PR 相關只當通知紀錄，不主動執行 merge 等寫入操作
- 會主動提醒壓縮記憶 `/compress` 並清理 daily session

## Agent model deltas (about the assistant/system)
- 今天 Sub-agent 犯錯後，main session 跟著接受錯誤結果而未完整驗證（Pattern: 只看 git log 不抽查內容）
- 分析維護者回覆時，Q8（Phase 4 依賴被跳過）和 Q9（BM25 lookup scope）第一輪被忽略，回頭查設計文件才發現
- 工具語法環境差異（PowerShell CRLF、&& 語法）造成錯誤訊息截斷，導致錯誤假設

## Lessons & pitfalls (symptom / cause / fix / prevention)
- Symptom: Sub-agent 完成後只檢查 git log 就往下走。 Cause: Sub-agent 逾時訊息讓 main session 誤以為「有 commit = 有產出」。 Fix: git show HEAD --stat + 抽查關鍵檔案 + 測試通過 才算成功。 Prevention: R12（Sub-agent 完成後完整驗證三步驟）
- Symptom: 執行 gh 命令但 repo 名稱不確定。 Cause: 沒有先確認目標 repo 就直接執行命令，PowerShell && 截斷錯誤訊息。 Fix: 聽到模糊專案名 → 先 gh search 或 memory recall 確認，不要用當前目錄猜測。 Prevention: 任何 gh/pr 操作前先確認 repo 名稱
- Symptom: Q8/Q9 在第一輪分析被忽略。 Cause: 只看本輪對話記錄，沒回頭查設計文件（pr246_proposal_ab_design_v8.md）。 Fix: 複雜架構問題 → 先查 memory/ 中的現有設計文件再做分析（R11）。 Prevention: R11
- Symptom: 每次討論串產生不同的工作指南檔案。 Cause: 沒有確立 canonical 檔案意識。 Fix: 確立 AI_WORKFLOW_MANUAL.md 為唯一 canonical，後續整合進去不新建檔案。 Prevention: canonical 檔案意識寫入 SOUL.md
- Symptom: 收到「PR 準備合併」就直接執行 merge。 Cause: 沒有區分「通知」和「指令」的語意邊界。 Fix: 「準備合併」= 純紀錄通知，不執行任何寫入操作。 Prevention: R1（通知 vs 指令的明確區分）

## Learning governance candidates (.learnings / promotion / skill extraction)
### Entry 1
**Priority**: high
**Status**: done
**Area**: config
### Summary
確立 AI_WORKFLOW_MANUAL.md 為唯一 canonical 工作指南檔案
### Details
今天 James 明確要求整合而非新建多份檔案。確認 `docs/AI_WORKFLOW_MANUAL.md` 為 canonical，未來所有工作流程規則統一寫入此檔。
### Suggested Action
在 SOUL.md 或 AGENTS.md 中加入「canonical 檔案意識」規則：討論串結論 → 整合進現有 canonical 檔，不新建。

### Entry 2
**Priority**: high
**Status**: done
**Area**: backend
### Summary
memory-lancedb-pro B-2 五個實作缺口（Q5-Q9）已張貼 Issue #538 等回覆
### Details
Q5（🔴 MMR 消滅 neighbors）、Q6（🔴 vectorOnlyRetrieval 無 BM25）、Q7（🟡 scope boundary）、Q8（🟡 Phase 4 依賴被跳過）、Q9（🟡 BM25 lookup input scope）
### Suggested Action
等維護者回覆 Q5-Q9 後才能實作 B-2。

### Entry 3
**Priority**: medium
**Status**: promoted_to_skill
**Area**: infra
### Summary
Sub-agent 犯錯 Pattern 分析 + 對應防禦規則已蒸餾完成
### Details
4 個 Pattern（P1: 未套用 workspace 規則、P2: 工具語法環境差異、P3: 確認/執行邊界模糊、P4: 逾時後 resume 不完整）已整理成 R12 和相關防禦對策，並寫入 .learnings/ERRORS.md。
### Suggested Action
下次派工 Sub-agent 前，主動提示對應的驗證檢查點。

## Open loops / next actions
- 等維護者回覆 Issue #538 Q5-Q9 後，確認 B-2 實作方向才能開始寫程式碼
- PR #500 等 maintainer（rwmjhb）合併
- memory-lancedb-pro PR #523 neighbor timestamp bug 後續確認
- PR #503/#523/#529 三個 PR 的疊加順序追蹤

## Retrieval tags / keywords
- memory-lancedb-pro, B-2, MMR, neighbor enrichment, Issue #538, Q5-Q9, vectorOnlyRetrieval, Phase 4 dependency
- AI_WORKFLOW_MANUAL.md, canonical, workflow guide consolidation
- Sub-agent validation, git show HEAD --stat, R12
- PR merge notification vs instruction, PR #500, maintainer
- PR splitting strategy, 疊加策略

## Invariants
- Always verify before acting: 用工具驗證事實，不靠假設（黃金法則）
- Always check repo name before any gh/pr command
- Never execute destructive operations (merge/close/edit) without explicit instruction
- When ambiguous project names appear → search/memory recall first, then act
- Sub-agent completion requires: git show HEAD --stat + content spot-check + test pass
- Complex architecture issues → check memory/ design docs before analysis
- Canonical file rule: consolidate into existing files, do not create new ones
- Important external replies → immediately structure into confirmed/pending categories

## Derived
- This run surfaced a new blocking issue: MMR's diversity removal may have already deleted candidate neighbors before B-2 insertion point (Q5), making the feature impossible to implement as currently designed
- The Phase 4 dependency skip (B-2 triggers only after B-1 is stable for 1 month) is a design-level assumption that was bypassed; maintainers have deprioritized B-1 in favor of B-2, so the trigger condition needs formal redefinition
- James's workflow preference is "single canonical file, not multiple per-thread" — this is a strong behavioral signal to encode in workspace rules
- The `/compress` workflow was successfully executed with dual-track output (LanceDB + active_state_discord.md), setting a good pattern for session-end hygiene
